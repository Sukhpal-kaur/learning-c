# Print Name Program

## Objective
To print my name on the screen using the C programming language.

## Program
(The program is available in `print_name.c`.)

## Output
Sukhpal Kaur

## Explanation
This program uses the `printf()` function from the `stdio.h` library to display my name on the screen. The `main()` function is the starting point of the program, and `return 0;` indicates that the program executed successfully.